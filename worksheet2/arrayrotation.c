#include <stdio.h>

int main(){
    int length;
    scanf("%d",&length);
    int array[length];
    for (int i=0;i<length;i++){
        int num;
        scanf("%d",&num);
        array[i] = num;
    }
    int num;
    print('Number to reverse by');
    scanf('%d',num);
    int temp = 
    for(int i = 0;i<length;i++){

    }
}